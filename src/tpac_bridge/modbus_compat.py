# -*- coding: utf-8 -*-
"""
[사용하지 않음] 이 파일은 pymodbus 버전 호환 계층이었다.

기존 robot.py 와 라이브러리를 통일하기로 하여 pyModbusTCP 기반의
modbus_io.py 로 대체되었다. 이 파일은 지워도 된다.
"""
raise ImportError("modbus_compat 는 modbus_io 로 대체되었습니다. "
                  "'import modbus_io' 를 사용하세요.")
