# ROS 2 colcon 빌드 산출물
build/
install/
log/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# 인계받은 원본 아카이브 (470MB)
erounsolution_project.zip

# Windows / WSL
*:Zone.Identifier
Thumbs.db

# 오피스 잠금 파일 (엑셀을 연 채로 두면 생긴다)
~$*

# 협력사(이로운솔루션/ERUT) 규격 원본 — 공개 저장소에 올리지 않는다.
# 코드와 문서는 파일 이름으로만 가리킨다. 필요하면 사내 공유 경로에서 받는다.
mqtt_test/ERUT-3S_*.xlsx
mqtt_test/ERUT-3S_*.pdf
