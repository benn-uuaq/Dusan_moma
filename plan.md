# 4-Steer AMR 개발 계획

## 1. 프로젝트 목표

4개의 바퀴를 독립적으로 조향할 수 있는 AMR(Autonomous Mobile Robot)을 ROS 2 기반으로 개발한다.

- 기준 OS: Ubuntu 22.04 LTS
- ROS 배포판: ROS 2 Humble
- 기본 언어: C++17, Python 3
- 빌드 시스템: `colcon`, `ament_cmake`
- 1차 목표: 수동 주행과 4륜 독립 구동·조향 제어
- 2차 목표: 원통 검출, 외곽 궤적 추종 및 원통 표면과 평행한 자세 유지
- 3차 목표: 3점 아웃트리거를 이용한 차체 수평 조절 후 리프트 제어
- 최종 목표: 원통형 설비 주변에서 안전하게 주행하고 작업할 수 있는 실내 AMR

## 2. 범위

### 포함

- 4륜 구동 및 4륜 조향 제어
- URDF/Xacro 로봇 모델
- 실차 하드웨어 인터페이스
- RViz 및 Gazebo Classic 시뮬레이션
- 조이스틱/키보드 수동 주행
- Odometry, TF, 센서 통합
- LiDAR와 거리 센서를 이용한 원통 검출, 거리 및 평행 자세 유지
- 비상정지, 통신 단절, 제어 이상에 대한 안전 정지
- IMU를 이용한 슬립 보정과 차체 수평도 측정
- 3점 아웃트리거 수평 제어
- 리프트 시퀀스 및 인터록 제어
- 단위·통합·실차 시험

### 초기 범위에서 제외

- 다중 로봇 관제
- 고속 주행
- 자동 충전 및 도킹
- ISO 3691-4 인증

## 3. 선행 결정 사항

구현 전에 아래 항목을 확정하고 문서화한다.

| 항목 | 결정할 내용 | 상태 |
|---|---|---|
| 구동 구조 | 4개 바퀴 독립 구동 | 확정 |
| 조향 구조 | 4개 바퀴 독립 조향. 일반 주행 중 조향각 변화 범위는 최대 ±30°. 크랩 주행, 전·후진 주행을 위한 제자리 방향 전환시 조향 각 제한 없음   | 부분 확정 |
| 구동 모터 | 모델, 감속비, 엔코더 분해능, 정격 전류 | 미정 |
| 조향 액추에이터 | 모델과 위치 제어 방식은 미정. 절대형 싱글턴 엔코더 사용 | 부분 확정 |
| 모터 드라이버 | 전체 EtherCAT 구성. Gazebo 검증 후 PLC와 연동 | 확정 |
| 메인 컴퓨터 | Intel NUC, 11세대 Core i5, Iris Xe 그래픽 | 확정 |
| PLC | KEYENCE PLC 사용 여부와 모델 | 확인 필요 |
| 센서 | 안전 LiDAR: UAM-05LP-T301, IMU: MW-AHRSv1, Intel RealSense 카메라 | 확정 |
| 거리 센서 | 원통과의 거리 및 평행도 측정용 센서 종류·수량·배치 | 미정 |
| 차체 제원 | 총중량 약 1,500 kg. 휠베이스, 윤거, 바퀴 반경 | 부분 확정 |
| 주행 성능 | 최대 속도 1 m/s, 최대 가속도 1 m/s², 정지 거리 0.6 m 이내. 회전 반경은 미정 | 부분 확정 |
| 아웃트리거 | 3점 지지 구조, 스트로크, 하중, 위치 피드백, 허용 수평 오차 | 미정 |
| 리프트 | 방식, 정격 하중, 스트로크, 속도, 상·하한 센서, 브레이크 | 미정 |
| 전원 | 배터리 전압, 용량, BMS, 차단 회로 | 미정 |

모든 길이 단위는 m, 각도는 rad, 속도는 m/s 및 rad/s를 사용한다.

## 4. 주행 모드

초기 구현은 복잡도를 낮추기 위해 모드별로 단계적으로 진행한다.

1. **정렬/정지 모드**
   - 구동을 정지하고 조향각만 제어한다.
   - 기동 시 조향 원점과 센서 상태를 검사한다.

2. **일반 4WS 모드**
   - 전륜과 후륜을 반대 방향으로 조향한다.
   - 작은 회전 반경의 Ackermann 계열 주행을 제공한다.

3. **크랩 모드**
   - 네 바퀴를 같은 방향으로 조향한다.
   - 차체 방향을 유지한 평행 이동을 제공한다.

4. **제자리 회전 모드**
   - 각 바퀴를 차체 중심의 회전 접선 방향으로 정렬한다.
   - 바퀴별 속도와 방향을 계산해 중심 회전을 수행한다.

각 모드 전환 시 먼저 속도를 0으로 만든 후 조향을 정렬한다. 주행 중 즉시 모드를 바꾸지 않는다.

## 5. 시스템 아키텍처

```text
teleop / task controller
      |
   cmd_vel
      |
4WS kinematics controller
      |
ros2_control controller
      |
hardware_interface
      |
motor driver / MCU
      |
drive motors + steering actuators

encoders + steering angle + IMU + LiDAR + distance sensors
      |
odometry / sensor fusion / cylinder perception
      |
TF + cylinder-relative pose

leveling controller <-> PLC <-> 3-point outriggers
lift controller    <-> PLC <-> lift actuator
```

### 제어 계층

- 상위 제어: 작업 시퀀스, 원통 외곽 추종, `cmd_vel`, 주행 모드 선택
- 운동학 계층: 차체 명령을 바퀴별 조향각과 회전속도로 변환
- 하위 제어: 조향 위치 제어, 구동 속도 제어, 상태 피드백
- 작업 제어: 아웃트리거 수평 조절과 리프트 시퀀스
- 안전 계층: E-stop, watchdog, 제한값 검사, 인터록, fault latch

Linux PC는 인지·운동학·작업 명령을 담당하고, PLC와 드라이버는 주기적인 하위 제어, 하드웨어 인터록 및 watchdog을 담당한다. ROS 2와 PLC 사이의 책임 경계와 안전 정지 권한은 구현 전에 확정한다.

## 6. ROS 2 패키지 구성안

```text
src/
├── amr_description/          # URDF/Xacro, mesh, ros2_control 태그
├── amr_bringup/              # 전체 launch 및 공통 parameter
├── amr_hardware/             # ros2_control hardware interface
├── amr_4ws_controller/       # 4WS 운동학과 controller
├── amr_msgs/                 # 주행 모드, 상태, fault 인터페이스
├── amr_odometry/             # 휠 odometry 계산
├── amr_localization/         # EKF, 센서 융합 설정
├── amr_cylinder_perception/  # 원통 검출과 상대 자세 추정
├── amr_cylinder_tracking/    # 외곽 추종과 거리·평행 제어
├── amr_leveling/             # IMU 기반 3점 아웃트리거 제어
├── amr_lift/                 # 리프트 시퀀스와 인터록
├── amr_plc_bridge/           # ROS 2와 KEYENCE PLC 연동
├── amr_simulation/           # Gazebo world, plugin, spawn launch
├── amr_safety/               # E-stop, watchdog, fault monitor
└── amr_test/                 # 통합 테스트 및 rosbag 시나리오
```

패키지는 역할별로 분리하되, 초기 단계에서는 불필요하게 세분화하지 않는다.

## 7. 핵심 인터페이스

### 표준 인터페이스

- 입력 속도: `/cmd_vel` (`geometry_msgs/msg/Twist`)
- 관절 상태: `/joint_states` (`sensor_msgs/msg/JointState`)
- 휠 오도메트리: `/wheel/odometry` (`nav_msgs/msg/Odometry`)
- 최종 오도메트리: `/odometry/filtered`
- IMU: `/imu/data`
- LiDAR: `/scan`
- 거리 센서: `/distance_sensors`
- 원통 상대 자세: `/cylinder/pose`
- 원통 추종 상태: `/cylinder/tracking_status`
- 차체 자세: `/imu/data`
- TF: `map -> odom -> base_link`

### 사용자 정의 인터페이스 후보

- `/drive_mode` 또는 mode 변경 service
- `/amr/status`
- `/amr/faults`
- `/safety/estop`
- `/safety/reset`
- `/leveling/command`, `/leveling/status`
- `/lift/command`, `/lift/status`
- `/plc/status`

주행 모드 변경은 수신 성공만 응답하지 않고, 정지 및 조향 정렬이 완료된 뒤 상태로 완료 여부를 확인할 수 있게 설계한다. 리프트 명령은 AMR 정지, 구동 비활성, 아웃트리거 접지, 수평도 만족, E-stop 정상 등의 인터록을 모두 만족할 때만 허용한다.

## 8. 좌표계와 관절 규칙

### 기본 TF

```text
map
└── odom
    └── base_footprint
        └── base_link
            ├── lidar_link
            ├── imu_link
            ├── *_steering_link
            └── *_wheel_link
```

- REP-103을 따른다: x 전방, y 좌측, z 상방.
- 바퀴 위치는 `front_left`, `front_right`, `rear_left`, `rear_right`로 통일한다.
- 조향 관절은 position command/state를 사용한다.
- 구동 관절은 velocity command와 position/velocity state를 사용한다.
- URDF의 관절 부호와 실물 엔코더 부호를 자동 시험으로 검증한다.

## 9. 운동학 및 제어 설계

### 입력

- 목표 선속도 `vx`, 필요 시 `vy`
- 목표 각속도 `wz`
- 주행 모드
- 각 바퀴의 차체 기준 위치 `(xi, yi)`

### 출력

- 바퀴별 목표 조향각
- 바퀴별 목표 회전속도

### 필수 처리

- 최소 조향 이동을 위한 각도 wrapping
- 조향각 한계와 구동속도 한계
- 조향 오차가 큰 동안 구동 억제
- 속도/가속도/jerk 제한
- 0 근처 명령의 deadband와 정지 판정
- 역회전 허용 시 조향각 180도 반전 최적화
- 특이점과 수치 불안정 처리
- 명령 timeout 시 안전 정지

운동학 공식은 차체 중심의 순간 회전 중심(ICR)을 기준으로 일반화하고, 각 주행 모드는 제한 조건으로 표현한다. 공식과 좌표계 정의는 구현 전에 별도 설계 문서와 단위 테스트로 고정한다.

## 10. 개발 단계

### Phase 0 — 요구사항과 하드웨어 확정

- [ ] 차체 치수와 질량 측정
- [ ] 목표 속도, 가속도, 회전 반경 정의
- [ ] 모터·드라이버·통신 방식 확정
- [ ] 센서와 컴퓨팅 플랫폼 확정
- [ ] 전원, E-stop, 브레이크 구조 결정
- [ ] 위험 분석 초안 작성

**완료 기준:** 하드웨어 블록도, I/O 목록, 성능 요구사항, 안전 요구사항이 승인됨.

### Phase 1 — 개발 환경과 로봇 모델

- [ ] Ubuntu 22.04 및 ROS 2 Humble 설치
- [ ] 저장소와 colcon workspace 구성
- [ ] CI에서 build, lint, test 실행
- [ ] URDF/Xacro 모델 작성
- [ ] joint limit 및 ros2_control 태그 추가
- [ ] RViz에서 TF와 관절 동작 검증

**완료 기준:** clean build가 가능하고 RViz에서 네 바퀴의 구동·조향 관절이 올바르게 움직임.

### Phase 2 — 운동학과 시뮬레이션

- [ ] 4WS 역운동학 구현
- [ ] 일반 4WS 모드 구현
- [ ] 크랩 모드 구현
- [ ] 제자리 회전 모드 구현
- [ ] 순운동학 및 odometry 구현
- [ ] Gazebo Classic 시뮬레이션 구성
- [ ] 단위 테스트와 궤적 오차 측정

**완료 기준:** 각 모드가 시뮬레이션에서 목표 궤적을 주행하고 오차가 합의된 허용값 이내임.

### Phase 3 — 하드웨어 인터페이스

- [ ] 통신 프로토콜과 상태 머신 정의
- [ ] ros2_control hardware interface 구현
- [ ] 조향 원점 및 calibration 구현
- [ ] 엔코더, 조향각, 전류, fault 수집
- [ ] 명령 timeout과 통신 watchdog 구현
- [ ] 저속 jack-up 시험

**완료 기준:** 바퀴를 띄운 상태에서 명령·피드백·정지·fault 처리가 안정적으로 동작함.

### Phase 4 — 실차 수동 주행

- [ ] E-stop과 물리적 전원 차단 시험
- [ ] joystick/keyboard teleop 연결
- [ ] 저속 직진, 후진, 회전 시험
- [ ] 주행 모드 전환 시험
- [ ] 조향·구동 PID 튜닝
- [ ] odometry 보정

**완료 기준:** 제한 구역에서 반복 주행 시 제어 불안정, 명령 잔류, 예상 밖 모드 전환이 없음.

### Phase 5 — 원통 검출과 외곽 추종

- [ ] LiDAR, 거리 센서, IMU, 카메라 장착 및 TF calibration
- [ ] LiDAR 기반 원통 중심·반경·상대 자세 추정
- [ ] 거리 센서를 이용한 원통 표면과의 거리·평행도 추정
- [ ] 센서 유효성 및 검출 신뢰도 판정
- [ ] 원통 외곽 목표 궤적 생성
- [ ] 거리와 평행 자세를 유지하는 추종 제어
- [ ] 원통 검출 손실 시 감속·정지·복구 정책 구현
- [ ] rosbag 기반 반복 평가

**완료 기준:** 지정된 원통 크기와 거리 범위에서 검출률, 거리 오차, 평행도 오차 및 궤적 추종 오차 목표를 만족함.

### Phase 6 — 아웃트리거 수평 제어와 리프트

- [ ] 3점 아웃트리거 기구학과 접지 순서 정의
- [ ] IMU 영점 및 차체 수평도 계산 구현
- [ ] 아웃트리거 개별 위치·하중 피드백 연동
- [ ] PLC 기반 수평 조절 상태 머신 구현
- [ ] 리프트 상·하한, 하중, 브레이크 및 비상 정지 연동
- [ ] `정지 → 아웃트리거 전개 → 수평 조절 → 리프트 허용` 인터록 구현
- [ ] 수평 실패, 접지 실패, 센서 이상 시 안전 복귀 구현
- [ ] 무부하 시험 후 단계별 하중 시험

**완료 기준:** 허용 경사와 하중 조건에서 목표 수평도를 달성하고, 모든 비정상 시나리오에서 리프트가 금지되거나 안전 정지함.

### Phase 7 — 작업 통합과 운영 준비

- [ ] 장시간 endurance test
- [ ] CPU, 메모리, 통신 지연 측정
- [ ] 원통 접근·추종·정지·수평 조절·리프트 작업 통합
- [ ] 센서·통신·모터·PLC fault injection
- [ ] 부팅 자동화와 systemd 구성
- [ ] 로그, rosbag, 진단 데이터 정책 수립
- [ ] 운영 및 복구 절차 문서화

**완료 기준:** 전체 작업 시나리오, 정의된 운용 시간 및 fault 시나리오를 통과하고 재부팅 후 정상 대기 상태로 복구됨.

## 11. 안전 원칙

- 소프트웨어 정지만으로 E-stop을 구성하지 않는다.
- 물리 E-stop은 모터 에너지를 안전하게 차단하도록 설계한다.
- 통신 단절 또는 명령 timeout 시 속도를 0으로 만든다.
- 센서값이 범위 밖이거나 조향 피드백이 불일치하면 주행을 금지한다.
- 아웃트리거 또는 리프트 동작 중에는 구동과 조향 명령을 차단한다.
- 아웃트리거 접지와 수평 상태가 확인되지 않으면 리프트 상승을 금지한다.
- 리프트가 안전 하강 위치가 아니면 아웃트리거 수납과 주행을 금지한다.
- fault reset은 원인이 제거되고 정지 상태일 때만 허용한다.
- 초기 실차 시험은 바퀴를 띄운 상태와 저속 제한 상태에서 시작한다.
- 자율주행 시험에는 즉시 개입 가능한 작업자와 물리 E-stop을 둔다.
- 안전 요구사항은 향후 ISO 3691-4 및 적용 지역 규정을 기준으로 별도 검토한다.

## 12. 시험 전략

### 단위 시험

- 직진, 곡선, 크랩, 제자리 회전 운동학
- 좌우·전후 대칭성과 부호 규칙
- 조향각 제한과 속도 제한
- 각도 wrapping과 역회전 최적화
- 0 명령과 timeout
- 순운동학과 역운동학의 상호 일관성

### 통합 시험

- `cmd_vel`부터 joint command까지의 연결
- joint state부터 odometry/TF까지의 연결
- mode transition state machine
- 드라이버 재접속과 fault recovery
- launch 및 parameter 유효성

### 실차 시험

- E-stop 정지 시간과 정지 거리
- 직진 편차와 반복 정밀도
- 원 궤적 반경 오차
- 크랩 이동의 차체 yaw 오차
- 제자리 회전 중심 편차
- 최대 적재 상태의 온도와 전류
- 원통 검출률, 거리 오차, 평행도 오차
- 원통 검출 손실 시 정지 동작
- 아웃트리거 수평도와 접지 안정성
- 정격 하중에서 리프트 인터록과 비상 하강
- 장시간 주행 안정성

각 시험은 조건, 명령, 기대 결과, 측정값, 합격 기준을 기록한다.

## 13. 초기 성능 지표

아래 값은 하드웨어 확정 후 수치화한다.

| 지표 | 목표 |
|---|---|
| 최고 선속도 | 1 m/s |
| 최고 선가속도 | 1 m/s² |
| 최고 각속도 | TBD |
| 정지 거리 | 0.6 m 이내 |
| 직진 10 m 횡방향 오차 | TBD |
| 360도 제자리 회전 위치 오차 | TBD |
| 경로 추종 RMS 오차 | TBD |
| 원통 검출률 | TBD |
| 원통 목표 거리 오차 | TBD |
| 원통 표면 평행도 오차 | TBD |
| 아웃트리거 수평도 오차 | TBD |
| 리프트 정격 하중 및 위치 오차 | TBD |
| 명령 timeout | TBD |
| 제어 주기 및 jitter | TBD |
| 연속 운용 시간 | TBD |

## 14. 주요 위험과 대응

| 위험 | 영향 | 대응 |
|---|---|---|
| 조향각과 구동속도 불일치 | 타이어 슬립, 전복 위험 | 조향 정렬 확인 후 구동 허용 |
| 엔코더/관절 부호 오류 | 예측 불가능한 움직임 | jack-up 자동 방향 시험 |
| Linux 제어 지연 | 불안정한 모터 제어 | 하위 제어를 MCU/드라이버에 배치 |
| 4WS 운동학과 원통 추종 제어 불일치 | 경로 추종 실패 | 주행 모드별 제어기 검증과 속도 제한 |
| 기계적 백래시 | odometry와 조향 오차 | calibration, 보정 모델, 센서 융합 |
| 통신 단절 | 명령 잔류 | 다중 watchdog과 하드웨어 정지 |
| 모드 전환 중 이동 | 급격한 차체 거동 | 정지-정렬-확인 상태 머신 |
| 원통 검출 손실 또는 오검출 | 충돌, 궤적 이탈 | 신뢰도 판정, 센서 교차 검증, 감속·정지 |
| 아웃트리거 불균등 접지 | 차체 기울어짐, 전복 | 개별 피드백, 수평 한계, 시간 초과 정지 |
| 리프트와 주행 동시 명령 | 전복, 설비 손상 | PLC 하드웨어 인터록과 ROS 상태 머신 |

## 15. 첫 번째 작업 목록

- [ ] 실제 차체의 휠베이스, 윤거, 바퀴 반경 입력
- [ ] 모터와 드라이버 통신 사양 입력
- [ ] 네 바퀴의 독립 구동 여부 확인
- [ ] 조향각 범위와 원점 검출 방식 확인
- [ ] 앞·뒤 조향 동기 규칙과 전·후진 제한 조건 확정
- [ ] 1,500 kg가 차체 자중인지 최대 적재 총중량인지 확인
- [ ] 거리 센서의 모델, 수량, 설치 위치 확정
- [ ] KEYENCE PLC 모델과 EtherCAT 구성도 확정
- [ ] 아웃트리거와 리프트 제원 및 안전 인터록 정의
- [ ] E-stop 전기 블록도 작성
- [ ] ROS 2 workspace와 패키지 skeleton 생성
- [ ] URDF/Xacro 초안 작성
- [ ] 4WS 운동학 요구사항과 테스트 벡터 작성

## 16. 변경 기록

| 날짜 | 내용 |
|---|---|
| 2026-07-01 | Ubuntu 22.04, ROS 2 Humble 기준 최초 계획 작성 |
| 2026-07-01 | 4륜 독립 구동·조향, EtherCAT/PLC, 원통 추종, 아웃트리거 수평 제어 및 리프트 요구사항 반영 |
